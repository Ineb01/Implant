#include <bits/stdc++.h> 
using namespace std; 

int main(int argc, char** argv )
{
    char command[100];
    while (true)
    {
        cin.getline(command, 100);
	    system(command);
    }
    return(EXIT_SUCCESS);
}